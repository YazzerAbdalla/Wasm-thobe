/**
 * @file user-role.enum.ts
 * @module UsersModule
 * @description Defines available user roles for RBAC (Role-Based Access Control).
 */

export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
}
