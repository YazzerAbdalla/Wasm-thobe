# وسم — WASM | منصة تصميم الثوب السعودي الفاخر

## اسم المشروع

**وسم (WASM) — منصة الثوب السعودي الفاخر**
> الاسم الظاهر في الواجهة والشعار: **«وسم / WASM»**
> العنوان الرئيسي في الصفحة: **«إرث من الإتقان»**
> الاسم التقني في الكود: `wasm-thobe` / `Thobe Platform — Premium Customization Experience`

---

## نبذة عن المشروع

**وسم** هي منصة ويب فاخرة لتصميم وطلب الثوب السعودي التقليدي عبر تجربة رقمية راقية.

يتيح للعميل بناء ثوبه خطوة بخطوة عبر **مصمم تفاعلي من 5 مراحل** (اللون → القماش → الياقة → الصدر والجيوب → المراجعة) مع **معاينة حيّة فورية** للثوب بتقنية SVG و CSS Variables بدون الحاجة لإعادة تحميل الصفحة. يختار العميل من **10 ألوان سعودية منتقاة** و **6 خامات فاخرة** (قطن مصري، بوبلين، أوكسفورد، كتان، صوف مبرد، خامة حريرية) مع تفاصيل دقيقة للياقة والأزرار والجيوب والأساور.

بعد كل اختيار يحصل العميل على **توصية ذكية للأناقة** (مثل: Royal Classic / Modern Casual) و **تسعير شفاف ومفصّل** قبل التأكيد. الطلب يعمل **كضيف بدون تسجيل**، مع تتبع للطلب، ولوحة تحكم للإدارة لإدارة الحالات وتصدير البيانات. المنصة تدعم أيضاً **مساعد ذكاء اصطناعي** للإجابة عن أسئلة الأقمشة والألوان وتوجيه العميل، مع تجربة بصرية فاخرة (خلفيات داكنة، لمسات ذهبية، زجاج ضبابي، وحركات سلسة).

باختصار: **نفس حرفية المشغل — ولكن بزجاج شفاف: ترى كل خيط وسعره قبل أن نبدأ.**

---

## أهم المميزات

- مصمم ثوب تفاعلي (5 خطوات) مع معاينة SVG حيّة
- 10 ألوان + 6 أقمشة + تفاصيل (ياقة، فتحة، أزرار، جيب، أسورة) + إضافات اختيارية
- محرك توصيات ذكي وتسعير ديناميكي
- طلب كضيف + تتبع الطلب + تأكيد فوري برقم طلب
- مساعد ذكاء اصطناعي (Anthropic Claude)
- لوحة تحكم للإدارة (إدارة الطلبات، تصدير CSV، إحصائيات)
- تصميم فاخر داكن — RTL عربي أولاً

---

## التقنيات والأدوات المستخدمة

### الواجهة الأمامية — `apps/frontend/`

| المجال | التقنية |
|---|---|
| الإطار | **React 19** + **Vite 7** |
| اللغة | **TypeScript 5.9** (strict mode) |
| التوجيه | **React Router DOM 7** |
| التصميم | **Tailwind CSS 4.1** |
| الحركة والانتقالات | **Motion (Framer Motion) 12** |
| إدارة الحالة | **Zustand 5** (للمصمم) + **React Context** (للمصادقة) |
| الاتصال بالخادم | **Axios 1.14** (مع اعتراض لتجديد التوكن) |
| النماذج والتحقق | **React Hook Form 7** + **Zod 4** + **Hookform Resolvers** |
| الأيقونات | **Lucide React** |
| الخطوط | **Noto Kufi Arabic** (للعناوين) + **IBM Plex Sans Arabic** (للنصوص) |
| الاختبار | **Playwright** (E2E) |

> ملاحظة: الوثائق الأصلية تذكر Next.js 14، لكن التطبيق المنفّذ حالياً مبني بـ Vite + React Router.

### الواجهة الخلفية — `apps/backend/` (حسب الوثائق)

| المجال | التقنية |
|---|---|
| الإطار | **NestJS** (معمارية وحدات — Modular) |
| اللغة | **TypeScript** (strict) |
| قاعدة البيانات | **PostgreSQL عبر Supabase** |
| ORM | **TypeORM** |
| المصادقة | **JWT** — Access Token (15 دقيقة) + Refresh Token (7 أيام) |
| التحقق | **class-validator** + **ValidationPipe** |
| الذكاء الاصطناعي | **Anthropic Claude API** (نموذج Haiku) |
| التوثيق | **Compodoc + JSDoc** |
| الاختبار | **Jest** (وحدة) + **Supertest** (تكامل) |

### قواعد البيانات

- `users`, `refresh_tokens`, `colors`, `fabrics`, `accessories`, `customizations`, `customization_accessories`, `orders`
- فهارس على `orders(user_id, status, created_at)` و `customizations(color_id, fabric_id)`

### النشر والاستضافة

| الخدمة | المنصة |
|---|---|
| الواجهة الأمامية | **Vercel** |
| الواجهة الخلفية | **Render** |
| قاعدة البيانات | **Supabase** |

---

## هوية المشروع

- **الألوان:** أسود فاخر `#0B0B0B` + ذهبي `#D4AF37` + رمادي هادئ `#8A8A8A`
- **الشعور:** أتيليه فاخر هادئ — تراث وحرفية بلمسة عصرية مينيمال
- **اللغة:** عربي أولاً، اتجاه RTL بالكامل
